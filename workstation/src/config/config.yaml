dataset:
  name: "cityscapes"
  train_imgs_dir: "../../data/bdd100k/images"
  train_masks_dir: "../../data/bdd100k/labels"
  img_type: "jpg"
  mask_type: "png"
  num_classes: 19
  class_labels:
  class_weights: [
      1.0, # road
      1.5, # sidewalk
      1.0, # building
      2.0, # wall
      2.0, # fence
      2.0, # pole
      3.0, # traffic light
      3.0, # traffic sign
      1.0, # vegetation
      2.0, # terrain
      1.5, # sky
      3.0, # person
      3.0, # rider
      1.0, # car
      3.0, # truck
      3.0, # bus
      3.0, # train
      3.0, # motorcycle
      3.0, # bicycle
    ]
  resize: [32, 16] # [width,height]

wandb:
  API_KEY: "960c7d1edaf53d6de7137290cfcbbc610b77fc67"
  silent: "true" # true or false

paths:
  cfg:
    Unet: "../config/Unet.yaml"
    SegFormer: "../config/SegFormer.yaml"
