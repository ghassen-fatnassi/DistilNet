training:
  depth: 2
  in_channels: 3
  start_filts: 4
  batch_size: 10
  epochs: 10
  lr: 0.001
  #only using reduce on plateau for now
  factor: 0.1
  patience: 10
  save: False
  save_dir: "../../models/Teachers/Unet"
  log_dir: "../../logs/Teachers/Unet"
distillation:
  depth: 2
  in_channels: 3
  start_filts: 4
  batch_size: 10
  epochs: 10
  lr: 0.01
  temperature: 3
  alpha: 0.8 #weight of the teacher loss
  #only using reduce on plateau for now
  factor: 0.1
  patience: 10
  save: False
  teacher_weight_dir: "../../models/Teachers/Unet"
  save_dir: "../../models/Students/Unet"
  log_dir: "../../logs/Students/Unet"
